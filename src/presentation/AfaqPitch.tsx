/**
 * Re-export for backwards compatibility (Tailwind/Webpack caches may still resolve this path).
 * Prefer: `import { JudgesPitchClient } from "@/components/pitch/JudgesPitchClient"`.
 */
export { JudgesPitchClient as AfaqPitch } from "@/components/pitch/JudgesPitchClient";
